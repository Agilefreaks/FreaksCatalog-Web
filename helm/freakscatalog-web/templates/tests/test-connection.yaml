apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "freakscatalog-web.fullname" . }}-test-connection"
  labels:
    {{- include "freakscatalog-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "freakscatalog-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
