import React from 'react';

function ResetButton() {
  return (
    <button
      className="filter__button-reset"
      type="button"
      onClick="onChange"
    >
      Reset
    </button>
  );
}

export default ResetButton;
